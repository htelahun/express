var config = {
host : "localhost",
user : "root",
pass : "root",
port : 8889,
database : "cooperStuff"

//kidsmode :false;
//var toRENDER = (config.kidsmode) ? 'main_kids' : 'home';

//re.render(toRender) {kidsmode: config.kidsmode}
};

module.exports = config ;
